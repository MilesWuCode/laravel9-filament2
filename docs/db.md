```sh
# docker
docker ps

# mysql container id
docker exec -it container-id bash

# link
mysql -u root -p
password

# create
CREATE DATABASE IF NOT EXISTS cms;
GRANT ALL PRIVILEGES ON `cms%`.* TO 'sail'@'%';
```
